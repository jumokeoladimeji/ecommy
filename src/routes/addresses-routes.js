const addressController = require('../controllers/address')
// const auth = require('../middleware/authentication');

// module.exports = (app) => {
//   app.route('/api/v1/users/:userId([0-9]+)/addresss') 
//     .post(auth.verifyToken, addressController.validate, addressController.create)
//     .getauth.verifyToken, (addressController.list);
//   app.route('/api/v1/users/:userId([0-9]+)/addresss/:addressId([0-9]+)')
//     .get(auth.verifyToken, addressController.getOne)
//     .put(auth.verifyToken, addressController.validateBeforeUpdate, addressController.update)
//     .delete(auth.verifyToken, addressController.destroy);
// }
